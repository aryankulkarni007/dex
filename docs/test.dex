# x = 5
# name = "hello"
# ratio = 3.14

# struct Point {
#     x: flt,
#     y: flt
#     @(self: Point) print -> abyss { }
# }

# @(a: int, b: int) add -> int { }

# x = 1 + 2 * 3
# y = 2 ^ 3 ^ 2
# z = x == 5

# x = !(5)
# y = !!(x)

struct Point {
    x: flt, y: flt

    @(self, new_x) set_x -> abyss {
        self.x = new_x
    }
}

@() main -> int {
    p = Point(1.0, 2.0)
    p:set_x(99.0)
    print(p.x)
}

